# CLAUDE.md

## Project

ClipSync — 跨平台剪贴板同步工具。Python 后端 (FastAPI + MQTT)，Android 客户端 (Kotlin)，支持 Windows / Linux / macOS / Android。

## Agent skills

### Issue tracker

GitHub Issues (`trdong19/ClipSync`). See `docs/agents/issue-tracker.md`.

### Triage labels

5 个标准标签：needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix. See `docs/agents/triage-labels.md`.

### Domain docs

单上下文布局，`CONTEXT.md` + `docs/adr/` 在仓库根目录。See `docs/agents/domain.md`.
